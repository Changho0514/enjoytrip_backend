<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.board.model.mapper.BoardMapper">

	<resultMap type="BoardDto" id="board">
		<result column="article_no" property="articleNo"/>
		<result column="subject" property="subject"/>
		<result column="user_id" property="userId"/>
		<result column="content" property="content"/>
		<result column="hit" property="hit"/>
		<result column="recommendation" property="recommendation"/>
		<result column="comment" property="comment"/>
		<result column="date" property="date"/>
		<result column="isnotice" property="isnotice"/>
	</resultMap>
	
	<select id="getBoard" parameterType="int" resultMap="board">
		select article_no, subject, user_id, content, hit, recommendation, comment, date, isnotice
		from board
		where article_no = #{articleNo}
	</select>
	
	<sql id="search">
		<if test="word != null and word != ''">
			<if test="key == 'subject'">
				and subject like concat('%', #{word}, '%')
			</if>
			<if test="key != 'subject'">
				and ${key} = #{word}
			</if>
		</if>
	</sql>
	
	<select id="list" resultMap="board" parameterType="boardParameterDto">
		select article_no, subject, user_id, content, hit, recommendation, comment, date, isnotice
		from board
		<where>
			<include refid="search"></include>
		</where>
		order by article_no desc
	</select>
	
	<insert id="write" parameterType="boardDto">
		insert into board (subject, user_id, content, date)
		values (#{subject}, #{userId}, #{content}, now())
	</insert>
	
	<update id="modify" parameterType="boardDto">
		update board
		set subject = #{subject}, content = #{content}
		where article_no = #{articleNo}
	</update>
	
	<delete id="delete" parameterType="int">
		delete from board
		where article_no = #{articleNo}
	</delete>

	
</mapper>